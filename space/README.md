# Streamlit app

The interactive version of the report, deployed as a Hugging Face Space.

    pip install -r requirements.txt
    streamlit run app.py

`voicehaul/` here is a copy of the package one level up, so the Space is a
self-contained Docker build. Keep them in sync when the harness changes.
